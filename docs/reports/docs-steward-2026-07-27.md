# Documentation audit and repair — 2026-07-27

`docs-steward`, **apply mode**, on branch `docs/governance-alignment` at `7f4e881`
(parent `b12b55d`). Fixes are committed on that branch; nothing was pushed and no
pull request was opened.

## 1. Scope

**What this repository is for** (the sentence every "does this earn its place"
judgement below is measured against): *give someone a working MII KDS module IG
project they can start filling with profiles and content, instead of setting up
tooling.*

**Where documentation lives.** 95 tracked Markdown files. 2 are inside the
vendored `ig-template/` mirror and were not read. Of the remaining 93: 44 are the
IG's own narrative pages (`input/pagecontent/`, `input/translations/de/pagecontent/`
— 22 English pages and their German mirrors), 2 are dated reports, and 47 are
project documentation (`README`, `AGENTS`, `CONTRIBUTING`, `CODE_OF_CONDUCT`,
`SECURITY`, `docs/**` including 14 recipes, `skills/**`, and the `README` in
`scripts/`, `tests/`, `input/`, `input/fsh/`, `input/fsh/rulesets/`,
`input/resources/`, `input/translations/`). Documentation also lives in
`.github/ISSUE_TEMPLATE/`, in workflow header comments, and in the
`sushi-config.yaml` placeholder header — all were read.

**Read-only for this run.** `ig-template/**` (machine-synced mirror, maintained by
`scripts/sync-ig-template.sh`); `docs/reports/**` (dated, immutable);
`CHANGELOG.md` (Release Please output, released entries); `output/`, `temp/`,
`template/`, `input-cache/`, `fsh-generated/`, `publisher.jar` (gitignored build
artefacts).

**Already decided, and therefore not findings.** `docs/open-tasks.md` and
`docs/project-status.md` were read before anything was judged. The deliberately
unnamed code owner / security contact / conduct contact; the deferred
`ig-registry` registration and package publication; the German prose in
`scripts/ig-stats.py` and `report-content.json`; the cross-repo documentation
duplication with `ig-template-mii-kds` (closed 2026-07-27); the decision that
`input/pagecontent/**` and `input/translations/**` name repository paths in prose
rather than linking them.

**Gates.** `node --test scripts/*.test.mjs scripts/*.template-test.mjs`,
`node scripts/convention-check.mjs`, `bash scripts/language-model-check.sh`,
`bash scripts/first-run-bootstrap.sh` (dry run), and parsing of every tracked YAML
and JSON file.

**Steps that returned nothing, and why**

- **Step 4 (assistant residue) returned little, not nothing** — 9 items, all
  fixed. The preceding cleanup on this branch (`df6ca22`, `790eaab`, `2b02a89`,
  `0abcdb4`) had already removed the retellings and the point-in-time
  transcripts; what remained was one dangling work-plan reference, one
  editing-session scope note, and five passages describing the page they sit on.
- **Attribution to an AI: nothing.** File content, `.github/ISSUE_TEMPLATE/*`,
  `.github/CODEOWNERS`, `.github/dependabot.yml` and all 11 workflow headers were
  searched for AI/model/tool authorship and "generated by" notes. The only hits
  are the *prohibition* in `AGENTS.md:47-51`, this skill's own subject matter, and
  "generated by the IG Publisher" (a build tool). There is no pull-request
  template, so there is no commit-trailer surface to leak attribution.
- **Step 5 (personal data) returned one item**, fixed. No individual is named as
  contact, owner or example anywhere else; `.github/CODEOWNERS` is deliberately
  empty and says so.

## 2. Fixed

| # | File | What |
|---|---|---|
| 1 | `docs/project-status.md:3,6` | Named `v0.2.0` as the released version. `v0.3.0` is the latest tag and GitHub release (2026-07-27); the "as of" stamp moved with it. |
| 2 | `docs/release.md:116` | Told a release manager to record the approval date on `metadata.html` — a file the IG Publisher generates. Now names `input/pagecontent/metadata.md` and its German mirror. |
| 3 | `docs/release.md:34-38` | Named four of the five files the first-run bootstrap removes; `CHANGELOG.md` was missing. |
| 4 | `docs/secrets.md:1` | Heading carried "(F + G)", a work-plan label that appears nowhere else in the repository. |
| 5 | `docs/release.md:238-239` | Ended a note with "this page does not modify go-publish" — a scope statement about an editing session. |
| 6 | `docs/release.md:15-18` | "Reference vs. recipe (keep the split clean): this file explains…" — the navigation is kept, the self-justification is gone. |
| 7 | `docs/concepts.md:4` | "Every non-obvious choice carries a **Why**." |
| 8 | `docs/workflows.md:12` | "every non-obvious point carries a **Why**." |
| 9 | `docs/further-reading.md:3-4` | "Each entry says **why** you would read it. Links were checked when this file was written" — an undated freshness stamp a reader cannot act on. |
| 10 | `docs/recipes/review-a-dependency-update.md:11` | "**Why a recipe:**" justified the page. The warning it carried ("updates are proposals, never automatic") is kept verbatim. |
| 11 | `skills/ig-analyze/SKILL.md:25-26` | "the inherited effort/planning forecaster has been removed from the tool" narrated a past change; the guardrail at `:92-93` already states what is. |
| 12 | `scripts/README.md:36` | "the language model this repository moved away from" — now states the model the guard protects, matching the script's own header. |
| 13 | `README.md:40`, `docs/recipes/create-a-new-module.md:19`, `docs/recipes/switch-template-to-published.md:61`, `docs/recipes/first-run-setup.md:187`, `docs/recipes/render-profiles.md:38` | Five "just"/"simply" that tell a stuck reader the step was easy, all at a first build or first bootstrap. |
| 14 | `input/fsh/rulesets/crmi.fsh:18` | A named third party's personal email address, in a file copied verbatim into every module created from this template. The port mapping it records is unchanged; the individual is no longer named. |
| 15 | `README.md:61` | Listed three of the four skills in `skills/`; `docs-steward` was missing. `AGENTS.md:88-105` already listed all four. |
| 16 | `docs/workflows.md:50` | Gave the preview build's branch filter as "except `main`/`gh-pages`"; `ig-publisher.yml:77-81` excludes `fsh-generated` too. |

Commits: `33b11fb`, `7661375`, `24f9434`, `8941ab9`, `7f4e881`.

**Gate results after the fixes** — 84/84 tests pass; `convention-check` PASS;
`language-model-check` clean; bootstrap dry run exits 0; 19 YAML + 11 JSON files
parse. `sushi .` on a placeholder-substituted copy of the post-fix tree reports
**0 errors, 0 warnings**, which is what verifies that the `crmi.fsh` comment edit
changed nothing the build reads.

## 3. Needs a human

### H1 — `switch-template-to-published.md` deletes one of three things, and says nothing else points at the folder

`docs/recipes/switch-template-to-published.md:96-103`:

> ```bash
> git rm -r ig-template
> ```
>
> This also removes `ig-template/README.md`. **Nothing else references the folder
> (the only pointer was `ig.ini`, which you just changed).**

20 tracked files reference `ig-template/`. Two of them break:
`.github/workflows/sync-ig-template.yml` and `scripts/sync-ig-template.sh`. Both
say so themselves — `sync-ig-template.yml:22-26`: *"Once you switch ig.ini to the
published package (docs/recipes/switch-template-to-published.md), **delete this
workflow, the ig-template/ folder and scripts/sync-ig-template.sh**"* — and the
recipe they point at never mentions either file (`grep -n -i sync` on it returns
nothing).

The consequence is concrete. `sync-ig-template.yml:32-34` triggers on
`pull_request: branches: [dev]` — exactly the PR step 6 tells you to open — and
runs `scripts/sync-ig-template.sh --check`, which at line 88 does
`diff -r --exclude=README.md "${tmp}/new" "${repo_root}/ig-template"` and at
line 95 exits 1 when that differs. With the folder deleted, the diff cannot
succeed, so the job emits `::error::ig-template/ has DRIFTED` and fails the PR
that step 6 says to "merge once green".

**Task.** In `docs/recipes/switch-template-to-published.md`, extend step 3 to
delete `.github/workflows/sync-ig-template.yml` and `scripts/sync-ig-template.sh`
alongside `ig-template/`, add them to the step 6 PR contents, and replace the
"nothing else references the folder" sentence with the list of what does. Confirm
against `sync-ig-template.yml:22-26` and `scripts/sync-ig-template.sh:19`, which
are the authority.

### H2 — both target-organisation repositories now exist, and are empty

`docs/open-tasks.md:63-64`: *"The two template repositories do not exist under
`medizininformatik-initiative` yet"*, and `docs/project-status.md:16`: *"Have they
moved yet? **No.**"*

Earlier in this run both `medizininformatik-initiative/mii-kds-module-template`
and `medizininformatik-initiative/ig-template-mii-kds` returned 404. Re-checked at
2026-07-27T16:16Z, both exist — created 15:56:58Z and 16:00:24Z, so **during this
run** — with `size=0`, `is_template=false`, default branch `main`, and no commits
(`git/trees/main` → *"Git Repository is empty"*).

The substance ("not migrated") still holds, but the failure mode of every
target-org link has changed from *404* to *resolves to a real, empty repository*,
which a reader cannot recognise as wrong. Two places are affected concretely:

- `SECURITY.md:9` sends vulnerability reporters to
  `https://github.com/medizininformatik-initiative/mii-kds-module-template/security/advisories/new`.
  Private vulnerability reporting is enabled on **both** repositories
  (`gh api …/private-vulnerability-reporting` → `{"enabled":true}` for each), so
  that link no longer fails — it accepts the report into the empty repository,
  where nobody maintaining this project will see it. A silently misdirected
  security report is worse than a broken link.
- `docs/recipes/first-build-in-devcontainer.md:26-28` tells the reader to click
  *Use this template* on that same URL. `is_template=false` there; the real
  repository, `forschungsgruppe-digital-health/mii-kds-module-template`, is the
  one with `is_template=true`.

**Task.** Decide whether the two empty repositories mean the migration is
imminent. If it is not, re-word `docs/open-tasks.md:61-72` and
`docs/project-status.md:16` from "do not exist" to "exist but are empty
placeholders", since the reasoning in that section rests on the links resolving
nowhere. Independently of that decision, point `SECURITY.md:9` and
`docs/recipes/first-build-in-devcontainer.md:27` at the repository that actually
serves them today.

### H3 — `metadata.md` says an extension is not enabled; it is

`input/pagecontent/metadata.md:87-95` (and `input/translations/de/pagecontent/metadata.md:90-98`):

> Not enabled in this scaffold, but prepared as commented blocks in `sushi-config.yaml`:
>
> - [Artifact Related Artifact](…) — a literature citation for the module.
> - [CQF Expansion Parameters](…) together with a [CRMI Manifest Parameters](…) resource and the `path-expansion-params` / `pin-manifest` parameters.

`sushi-config.yaml:206-208` is uncommented and active, and the comment directly
above it (`:203-205`) says *"Active together with path-expansion-params /
pin-manifest below — all three must name the same resource."* `input/resources/README.md:49`
agrees: *"Three places in `sushi-config.yaml` reference this file — all three are
active."* Only `artifact-relatedArtifact` (`sushi-config.yaml:189-198`) is
commented out. These are rendered pages, so the error ships to readers of the
built guide in both languages.

**Task.** Move the CQF Expansion Parameters bullet out of the "not enabled" list
in `input/pagecontent/metadata.md` and in the German mirror, keeping the two
files in step.

### H4 — a best-practice check passes on evidence that is not true

`docs/ig-best-practices-checklist.md:103`:

> `| Examples avoid declaring meta.profile unless required | ✅ | The shipped example does not declare it |`

`input/fsh/instances/example-patient-instance.fsh:26` is
`* insert MetaProfile(https://www.medizininformatik-initiative.de/fhir/modul-{{MODULE_SLUG}}/StructureDefinition/example-patient)`,
and `input/fsh/rulesets/meta-profile.fsh:14` sets `* meta.profile[+] = "{canonical}|{{CALVER_VERSION}}"`.
`input/fsh/rulesets/README.md:27` documents `MetaProfile` as pinning `meta.profile`
on examples. The example does declare it.

Not auto-fixed because correcting the evidence changes what the row asserts —
either the ✅ is wrong, or "unless required" applies here and the evidence should
say why.

**Task.** Decide which, and restate the row. If pinning `meta.profile` on the
shipped example is deliberate (version-pinned examples are an MII convention),
say so in the evidence cell instead of denying it.

### H5 — the docs index promises a list that does not exist

`docs/README.md:41-42`:

> The exceptions — things that genuinely fail a build here — are stated as such in
> [workflows.md](workflows.md).

`docs/workflows.md` has no such statement. Its headings are *Branching*, *Layer 1*,
*Layer 2*, *The toggle summary*, *Release*, *Secrets*; `grep -i fail` returns two
lines, one table cell about `sync-ig-template.yml` and one "skips instead of
failing" note. The hard gates are stated elsewhere — `docs/release.md:72` (the
reusable validation is the authoritative error gate),
`docs/ig-best-practices-checklist.md:124-127`, `skills/wiki-consistency-check/SKILL.md:40-48`
(convention-check hard-fails on a violation) — but nowhere in one place.
`docs/README.md` was added one commit before this run, so the promise is new.

**Task.** Add a short "What actually fails a build" section to `docs/workflows.md`
collecting the reusable FHIR validation gate, `convention-check` (hard on
`release/**`), `language-model-check`, the `sync-ig-template` drift check and the
`toolchain-pins` test — or narrow the sentence in `docs/README.md` to point at the
places that do state them.

### H6 — four placeholder walkthroughs omit `input/resources/`

`sushi-config.yaml:11-14` lists where the 19 placeholders occur and includes
`input/resources`. Four prose walkthroughs do not: `README.md:29-34`,
`docs/recipes/create-a-new-module.md:35-39`, `docs/recipes/first-run-setup.md:132-138`,
and the checklist `scripts/first-run-bootstrap.sh` prints (`:102-114`).
`input/resources/Parameters-expansion-manifest.json` carries `{{MODULE_SLUG}}` in
its resource `id`, and `input/resources/README.md:59-64` says an unreplaced
placeholder there *"is not a valid id and the IG Publisher will reject it."*

The `grep` sweep at `docs/recipes/create-a-new-module.md:41-46` catches it because
its scope is the whole `input` tree — so the backstop works, but the prose list a
reader follows first does not.

**Task.** Add `input/resources/` to the four lists, or replace the enumerations
with "work through the files the `sushi-config.yaml` header names, then run the
sweep" and let the header be the single home.

### H7 — every link in the released changelog is dead, and half of them return 200

`CHANGELOG.md:3-23` carries 25 unique `github.com/medizininformatik-initiative/mii-kds-module-template/…`
links — 12 issue links, 12 commit links and one compare link. None of them reaches
what it names, because that repository was created empty today (H2). But only the
12 issue links return 404. The 12 commit links and the compare link return **HTTP
200**, serving the empty repository's landing page: the fetched `<title>` is
`medizininformatik-initiative/mii-kds-module-template · GitHub`, where the same
commit on the real repository titles itself
`feat: add dev container and first-build recipe (#3) · forschungsgruppe-digital-health/mii-kds-module-template@3a4baa5 · GitHub`.

A status-code link checker therefore passes 13 of the 25. This is worth recording
on its own: any future link checking in this repository has to compare content,
not response codes.

The `0.3.0` entry on `origin/main` uses the `forschungsgruppe-digital-health`
URLs, so Release Please generates them correctly now; only the released `0.2.0`
entry is affected.

This report does not fix it: a changelog entry for a released version is a
published, dated artefact. `CHANGELOG.md` is also removed from every created
module by the bootstrap, so the damage is confined to this repository's own public
page.

**Task.** Decide whether to leave the `0.2.0` entry as the historical record it is
(the links resolve again after a repository transfer, since GitHub redirects), or
to correct it deliberately and note the correction in the entry.

### H8 — a licence attribution points nowhere

`skills/ig-analyze/SKILL.md:21-24` attributes the skill to
`medizininformatik-initiative/mii-kds-sample-ig-inoffiziell` under CC-BY-4.0. That
URL returns 404 and the repository is not in the organisation's repository list.
CC-BY requires attribution; an attribution that resolves nowhere does not give it.

**Task.** Find where the sample IG went (renamed, made private, or deleted) and
either update the link or state in the attribution that the source is no longer
public, with the date it was consulted.

### H9 — `tests/README.md` understates what the CI self-check substitutes

`tests/README.md:70-73`:

> The template repo's CI self-check substitutes placeholders **only into
> `sushi-config.yaml`, `ig.ini` and the narrative pages** — not into `tests/`

`.github/workflows/ig-publisher.yml:147-212` substitutes `sushi-config.yaml`,
`ig.ini`, and then every `*.md`, `*.xml`, `*.fsh` and `*.json` under `input/` —
FSH sources and `input/resources/*.json` included. The workflow's own comment at
`:191-193` says "EVERY build input … not just the narrative pages", while its
comment at `:172-174` and its `::notice` at `:212` still say "the narrative
pages". The "not into `tests/`" half is correct.

**Task.** Correct the scope in `tests/README.md:70-73` and in the two stale
phrasings inside `ig-publisher.yml`, so all three match `:191-193`.

### H10 — `translations/` is described as gitignored and is not

`input/translations/README.md:34-36`: *"The IG Publisher generates translation
templates for every resource into `translations/de/po/` (repo root, **gitignored**)
on each build."* `.gitignore` (23 lines) has no `translations` entry. The generated
output is untracked *and* unignored, so it shows up in `git status` after a build.

**Task.** Either add `translations/` to `.gitignore` (making the sentence true) or
correct the sentence. The first is likelier to be what was meant.

### H11 — the repository's headline acronym is never expanded

`README.md:1-3` opens with "mii-kds-module-template" and "MII Kerndatensatz (KDS)
module". "MII" is never expanded in `README.md`, `AGENTS.md`, `CONTRIBUTING.md` or
anywhere in `docs/**`; the expansion exists only inside the IG's own narrative
(`input/pagecontent/index.md:15`, "Medical Informatics Initiative (MII)"), which a
reader of the repository does not open. `docs/glossary.md` has no MII entry, and
none for "Kerndatensatz" as a translatable word. The glossary's own opening
promises "plain-language definitions of the terms you meet in this repository".

**Task.** Expand MII on first use in `README.md:3` and add glossary rows for MII,
Kerndatensatz and TF KDS. The same pass could cover the other terms a stranger
meets before any definition — CI, preview, QA, `gh-pages`, scaffold, Zulip.

### H12 — no pull-request template, and none published to adopt

`.github/` has `CODEOWNERS`, `ISSUE_TEMPLATE/` (three forms plus `config.yml`),
`dependabot.yml` and 11 workflows, and no `PULL_REQUEST_TEMPLATE.md` in any
casing. Checked before proposing one: `forschungsgruppe-digital-health/.github`
does not exist (404), and `medizininformatik-initiative/.github` contains only
`README.md`, `images` and `profile` — so nothing is published at either
organisation to adopt instead.

**Task.** Decide whether to add one. If so, it should carry what `AGENTS.md:32-51`
and `CONTRIBUTING.md` already require — Conventional Commit title, target `dev`,
green CI, no AI attribution — and it will propagate into every created module,
which is an argument both for adding it and for keeping it short.

### H13 — the drift matrix records the wiki's release target without noting this repository's divergence

`skills/wiki-consistency-check/references/check-matrix.md:100` says modules go
`release/vYYYY.n.n` → PR to `main`. I verified this against the primary source:
the MII wiki page *Module Release Workflow*, fetched 2026-07-27, says *"Open a
Pull Request from `release/v2025.0.1` to `main`"*. The matrix is correct.

But `docs/release.md:161-162` has this scaffold open that PR into `dev` and then
promote `dev` → `main`, and explains why at `:92-96`. The matrix row notes the
SemVer/CalVer divergence for template repos and not this one, so a
`wiki-consistency-check` run can report the scaffold as drifting when it is
deliberately diverging.

**Task.** Add to that row that this scaffold routes the release PR through `dev`
per `CONTRIBUTING.md`, and that the divergence is documented — the same treatment
the row already gives the SemVer/Release Please case.

### H14 — "the same helper lives at the same path" is not true of the helpers the paragraph is about

`scripts/README.md:7-10` explains that `go-publish.yml` calls
`automation/scripts/...`, "so the path is a contract with the publication
workflow. The same helper lives at the same path in `ig-template-mii-kds`."

The sibling repository's `scripts/` (read from its `main` tree) contains
`README.md`, `check-language-model.sh`, `check-updates.mjs`,
`check-updates.test.mjs`, `narrative-table-styles.test.mjs`,
`set-su-termserv-secrets.sh`, `terminology-fallback.test.mjs`, `trace-logo.sh` —
none of the seven publication-automation helpers the paragraph is about. Two
shared helpers *are* at the same path; the language guard is at
`scripts/check-language-model.sh` there and `scripts/language-model-check.sh` here.

**Task.** Say which helper is meant, or drop the sentence — the path contract with
`go-publish.yml` stands on its own.

### H15 — three directory READMEs nothing links to

`input/README.md`, `input/resources/README.md` and `input/translations/README.md`
have zero inbound Markdown links; the first and third have no plain-text mention
anywhere either. They are reachable by browsing the directory on GitHub, so this
is not fatal — but `input/resources/README.md` holds the CalVer → SNOMED CT
release table that exists nowhere else, and the `{{MODULE_SLUG}}` warning that H6
shows is missing from four walkthroughs.

**Task.** Link `input/README.md` from the "Where things live" table in
`README.md:47-62`, and `input/resources/README.md` and
`input/translations/README.md` from `input/README.md`.

## 4. Declined

Each of these looks like a finding and is not. Recorded so the next run does not
raise it again.

1. **14 "broken" links in `input/pagecontent/downloads.md` and its German
   mirror** (`../package.tgz`, `../full-ig.zip`, …). They resolve. The IG renders
   its pages one level down, under `/en/` and `/de/`, and the artefacts sit at the
   site root; `en/downloads.html` in the built preview emits `href="../package.tgz"`
   and `package.tgz` is present. A checker that resolves page-content links against
   the source tree reports all 14 as broken. Confirmed by extracting every internal
   `href` from the 44 authored pages in the built site for this branch
   (`origin/gh-pages:branches/docs/governance-alignment/`): **2007 links, 0 broken.**
2. **`input/translations/README.md:42` → `../../docs/recipes/add-translation.md`.**
   Not page content — a repository README that happens to live under `input/`. The
   link resolves on the filesystem.
3. **`href="#"` in `docs/recipes/add-translation.md:82,179`.** Prose quoting the
   value the menu QA check rejects, not a link.
4. **"No issue templates" from the GitHub community-profile API.** The endpoint
   reports `issue_template: null` for this repository, which has
   `.github/ISSUE_TEMPLATE/` with four files. Control run as the skill requires:
   the same endpoint returns `null` for `forschungsgruppe-digital-health/risk-management-template`
   and `…/arc42-project-template`, both of which have the directory. The payload's
   `files` keys are `code_of_conduct`, `code_of_conduct_file`, `contributing`,
   `issue_template`, `license`, `pull_request_template`, `readme` — it says nothing
   at all about a security policy, support pointer or citation file. Any absence
   derived from it is manufactured. (`pull_request_template: null` *was*
   corroborated against the filesystem — that is H12.)
5. **`https://confluence.hl7.org/display/FHIR/IG+Publisher+Documentation` returns
   405.** It returns 200 with a browser user-agent; the 405 is bot rejection.
6. **`tests/README.md:23` documents `tests/profiles/invalid/`, which does not
   exist.** It is a layout diagram of what the harness expects, and the manifest
   schema below it documents `invalid/<file>.json` entries. The directory is one a
   module creates; `profile-test-cases` ships commented out, so nothing runs
   against it.
7. **`docs/ig-best-practices-checklist.md:37` counts "the Conformance section
   (4 pages)" where `sushi-config.yaml:283-292` nests five.** Line 33 of the same
   file partitions the pages as `conformance.md`, `general-requirements.md`,
   `must-support.md`, `missing-data.md` (normative) with `security-and-privacy.md`
   given its own check at line 38. The count is consistent with how the file
   itself partitions them.
8. **`docs/reports/e2e-module-creation-2026-07-23.md:20` contradicts today's
   `REMOVE=` line** — it names `docs/recipes/first-run-setup.md` and
   `tools/first-run-bootstrap.sh` as removed, when both are now on the
   NEVER-protect list and the `tools/` directory no longer exists. A dated report
   is expected to go stale; `docs/reports/README.md:9-14` says exactly this. Not a
   defect, and not editable.
9. **`docs/open-tasks.md:128` "That was once real duplication; it is not any
   more."** Reads as narration, and is the record that stops the next audit
   reopening a question closed on 2026-07-27.
10. **`input/pagecontent/version-history.md:22-23` offers `cibuild` as a version
    label while `AGENTS.md:58-61` forbids it.** Different objects: the page
    describes the module's own release label, the guardrail scopes itself to
    "dependencies, the IG template pin, tool versions", and both
    `scripts/convention-check.mjs:71` and `.github/workflows/go-publish.yml:283`
    match only pin syntax.
11. **`docs/recipes/first-run-setup.md:43` "just skip the part that creates
    `dev`".** Restrictive "just" meaning *only*; deleting it changes the
    instruction. The other five were dismissive and were deleted.
12. **No `CITATION.cff`.** Authoring one requires naming authors, which
    `docs/open-tasks.md:25` deliberately defers while the repository is a
    prototype.
13. **The 44 IG narrative pages have "no inbound links".** They are wired through
    `pages:` in `sushi-config.yaml:268-328` and `input/includes/menu.xml`, not
    through Markdown links.
14. **German prose in `skills/ig-analyze/references/metrics-catalog.md` and the
    migration framing of its recommendation rows.** Recorded at
    `docs/open-tasks.md:102-115` as known and deferred.
15. **`scripts/first-run-bootstrap.sh` has no mechanical guard against `--apply`
    being run inside the template repository itself.** The documentation is
    correct and repeats the scoping three times (`README.md:21-25`, the script
    header `:1-5`, `docs/recipes/create-a-new-module.md:13-21`). A hardening idea
    for the script, not a documentation defect.

## 5. Could not verify

1. **`docs/open-tasks.md:94-96` — "It was tested against 20 phrasings and catches
   every wording that has actually occurred here."** No test artefact exists:
   there is no `scripts/language-model-check.test.mjs` and no phrasing corpus
   anywhere in the tree. Its `PATTERNS` array holds 12 entries, which is not in
   itself a contradiction — one pattern can cover several phrasings — but nothing
   in the repository lets the "20" be re-derived, so the claim rests on the memory
   of whoever wrote it. (The absence of a companion test is *not* distinctive:
   `ig-translate.sh`, `resolve-ig-template-source.sh`, `set-su-termserv-secrets.sh`,
   `sync-ig-template.sh` and `ig-stats.py` have none either; 11 scripts do.)
2. **`skills/ig-translate/references/translate-spec.md:3` — "As of 2026;
   empirically verified with IG Publisher 2.2.7/2.2.8".** Its own
   `skills/ig-translate/SKILL.md:46` says "Re-verified with IG Publisher 2.2.11",
   every build workflow pins `2.2.11`, and `SKILL.md:64-66` instructs that the
   table be re-verified whenever the pin changes. I cannot tell whether the
   reference file's claims were re-verified and the stamp forgotten, or not
   re-verified. It also contradicts `docs/recipes/add-translation.md:136` on
   whether `CodeSystem.concept.display` is translatable. Reconciling it needs an
   IG Publisher build, which this run could not do (below).
3. **The renders / does-not-render translation table.** Needs a full IG Publisher
   build to confirm.
4. **`docs/secrets.md:36-49`** — the recorded `openssl s_client` handshake against
   `ontoserver.mii-termserv.de` and the HTTP 400 without a client certificate. Not
   probed: it needs credentials and reaches a live MII server.
5. **`docs/open-tasks.md:30-41`** — the claimed upstream defects in
   `kerndatensatz-basis` and `HL7/ig-template-base2`. External repositories, not
   fetched.
6. **`skills/wiki-consistency-check/references/check-matrix.md:105-115`** — "Wiki
   pages (verified to exist, 2026-07-21)". Of the seven, only *Module Release
   Workflow* was fetched during this run (for H13); the other six were not.

### Walk skips (step 9)

The documented path was walked as a first-time user on a fresh clone. Recorded as
**skips, not findings**:

- **The dev container could not be started** — no Docker daemon on this host. So
  `docs/recipes/first-build-in-devcontainer.md` steps 4-7 are unverified, and the
  six version checks at `:73-78` were confirmed against
  `.devcontainer/devcontainer.json` and the workflow pins instead of by running
  them. All six agree (Java 17 base image, Node 22.23.1, SUSHI 3.20.0, Ruby
  3.3.12, Jekyll 4.4.1, publisher 2.2.11 + its SHA-256).
- **The full IG Publisher build was not run** — no Jekyll on this host. `sushi .`
  was run instead, twice.
- **"Use this template" was not clicked** and `--apply` was not run: both act on a
  real GitHub repository. The bootstrap dry run was executed, and its documented
  removal list was applied locally in a scratch clone, as the 2026-07-23 e2e report
  did.

**How far the walk got, and it is a result:** from a fresh clone to a module named
`person` at CalVer `2026.0.0` that compiles — bootstrap dry run prints its five
removals with a reason each; the recipe's verification command
(`ls release-please-config.json …`) prints nothing, as documented; placeholder
substitution across 71 files; the recipe's placeholder sweep
(`grep -rnE --exclude=README.md …`) comes back empty, as documented;
`node scripts/convention-check.mjs` turns all six PARAM rows to PASS; `sushi .`
reports **0 errors, 0 warnings**. No point in that sequence required opening a
file the documentation had not named, and no step required asking someone.

The experienced-reader walk found how to make a change (`CONTRIBUTING.md`, reached
from `README.md:73`), the conventions (`CONTRIBUTING.md` + `AGENTS.md`), and how a
release happens (`docs/release.md` + `docs/recipes/cut-a-release.md`, reached from
`docs/README.md:14`). It failed on one of the four questions: *what will automation
do to my contribution* — see H5.

## 6. Proposed issues

This run was non-interactive, so nothing was created. `gh` is authenticated and
this repository accepts issues (one open: #45, *Dependency status 2026W31*,
label `dependencies`); a search of open and closed issues found none covering the
findings below. If a maintainer wants them tracked, these are the titles, one per
finding, all under a `docs-steward` label:

1. `switch-template-to-published leaves the sync workflow behind and fails the PR it tells you to open`
2. `Both target-org repositories now exist and are empty — SECURITY.md and the devcontainer recipe point at them`
3. `metadata.md lists CQF Expansion Parameters as not enabled; sushi-config.yaml has it active`
4. `Best-practice checklist passes meta.profile on evidence the shipped example contradicts`
5. `docs/README.md promises a "what fails a build" list that workflows.md does not have`
6. `Four placeholder walkthroughs omit input/resources/, where an unreplaced slug is an invalid FHIR id`
7. `All 25 links in the v0.2.0 changelog entry are dead, and 13 of them answer 200`
8. `The ig-analyze CC-BY attribution points at a repository that returns 404`
9. `tests/README.md and two ig-publisher.yml comments understate the self-check substitution scope`
10. `input/translations/README.md calls translations/ gitignored; .gitignore has no such entry`
11. `MII is never expanded, and the glossary has no entry for it`
12. `Decide whether this template ships a pull-request template`
13. `check-matrix records the wiki's release target without noting this scaffold's documented divergence`
14. `scripts/README.md claims a helper parity with ig-template-mii-kds that does not hold`
15. `Three directory READMEs under input/ have no inbound links`
